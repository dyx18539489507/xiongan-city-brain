# ADR 0010：独立路侧 RSU 服务

状态：接受（2026-08-06）

## 决策

将路侧感知校验从 edge-service 拆为独立 `rsu-service` 运行角色。SUMO runner 发布原始观测，RSU 校验并以独立来源和单调序号重发，Edge 负责控制与安全，Cloud 只发布区域目标。

## 结果

组件边界与物理 MQTT 主题可以单独测试、重启和观测，且不会赋予 RSU 绕过边缘直接切灯的权限。
